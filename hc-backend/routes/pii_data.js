// "use strict";

// const piiDataController = require("../controllers/pii_data")
// , jwt = require("jsonwebtoken")
// , auth = require("./auth");

// module.exports = app => {
// 	// PiiData endpoints
// 	// app.get('/api/piidata', auth.isAuthenticated, piiDataController.piiDataList);
// 	// app.get('/api/piidata/:id', auth.isAuthenticated, piiDataController.piiDataRead);
// 	// app.post('/api/piidata', auth.isAuthenticated, piiDataController.piiDataCreate);
// 	// app.put('/api/piidata/:id', auth.isAuthenticated, piiDataController.piiDataUpdate);
// 	// app.del('/api/piidata/:id', auth.isAuthenticated, piiDataController.piiDataDelete);

// 	// app.get('/api/piidata/by-email/:email', auth.isAuthenticated, piiDataController.piiFindByEmail);
// };
