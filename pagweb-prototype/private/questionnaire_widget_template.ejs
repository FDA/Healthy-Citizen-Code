<adp-content-container>

    <adp-section-container>
        <adp-section-title>Questionnaire</adp-section-title>
        <hc-widget
          ng-if="!vm.loading && vm.fhirId"
          widget-id="{{'test-201804020319'}}"
          fhir-id="{{vm.fhirId}}"
                udid="{{vm.udid}}"
          url="{{vm.url}}"
                hc-research-url="{{vm.hcResearchUrl}}"
        >
        </hc-widget>

        <p ng-if="!vm.loading && !vm.fhirId">No Deidentified User ID found.</p>
    </adp-section-container>

</adp-content-container>
