<hc-content-container>

    <hc-section-container>
        <hc-section-title>Please avoid using this widget as it overloads openFDA.</hc-section-title>
        <hc-widget
            ng-if="!vm.loading && vm.fhirId"
            widget-id="{{'test-1523524547983'}}"
            fhir-id="{{vm.fhirId}}"
            hc-research-url="{{vm.hcResearchUrl}}"
            fhir-data-url="{{vm.fhirDataUrl}}"
            patient-data="vm.patientData"
            url="{{vm.url}}"
        >
        </hc-widget>

        <p ng-if="!vm.loading && !vm.fhirId">No fhir id found.</p>
    </hc-section-container>

</hc-content-container>
