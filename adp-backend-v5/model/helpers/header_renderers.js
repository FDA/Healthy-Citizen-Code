/**
 * HeaderRenderers ares used to render header if form fields for complex types(Object, Array) and Groups
 *
 * @param {object} args
 *
 * args.fieldData
 * args.formData
 * args.fieldSchema
 * args.pathToDat
 */

module.exports = () => {
  const m = {};
  return m;
};
