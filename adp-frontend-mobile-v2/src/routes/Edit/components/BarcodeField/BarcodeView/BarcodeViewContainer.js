import {connect} from 'react-redux';
import BarcodeView from './BarcodeView';

const mapDispatchToProps = {
};

const mapStateToProps = (state) => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(BarcodeView);
